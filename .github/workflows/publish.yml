name: CI

on:
  push:
    branches: [ docs ]
  pull_request:
    branches: [ docs ]
    
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: echo Hello, world!

      - name: Build
        run: echo Build!